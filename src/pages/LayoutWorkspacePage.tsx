import { AdminLayoutStudio } from './admin/AdminLayoutStudio'

/** Shown at `/admin/canvas?view=layout` — uses `LayoutWorkspaceProvider` from `CatalogLayout`. */
export function LayoutWorkspacePage() {
  return <AdminLayoutStudio />
}
