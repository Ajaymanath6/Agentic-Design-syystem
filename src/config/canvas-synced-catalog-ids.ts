/**
 * Catalog IDs that may be linked to canvas state (parity with Angular idea).
 * Extend when wiring catalog ↔ admin canvas selection.
 */
export const CANVAS_SYNCED_CATALOG_IDS: readonly string[] = [
  'catalog-home',
  'catalog-new',
]
